{
  "scripts": {
    "setup": "yarn install && yarn --cwd db install && yarn --cwd nuxt install && yarn --cwd backend install",
    "start": "run-p start:*",
    "start:frontend": "yarn --cwd nuxt dev",
    "start:backend": "yarn --cwd backend start",
    "start:database": "yarn --cwd db start"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
